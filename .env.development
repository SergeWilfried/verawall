# Base URL of the Go ingest server's console API (see fraud-ingest-server-go).
# Start it on :8080 and seed demo data with the conformance suite:
#   cd ../vera-tools/fraud-ingest-server-go && go run . &
#   node ../vera-tools/fraud-ingest-server/simulate-sdk.js all http://localhost:8080
VITE_API_BASE=http://localhost:8080
