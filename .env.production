# Build-time config for production deploys (Vite inlines these at build).
# Point the console at the deployed Go ingest server, then `pnpm deploy:cf`.
# Not a secret — this URL ships in the bundle either way.
VITE_API_BASE=https://vera-server-2d4q.onrender.com
